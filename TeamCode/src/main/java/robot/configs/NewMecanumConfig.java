package robot.configs;

public class NewMecanumConfig extends RobotConfig{
    {
        define(
                drive,
                gyro,
                odometry,
                camera,
                carousel,
                intake,
                lift,
                outtake,
                color
        );
    }
}
